# Database
DATABASE_URL=postgresql://democracy:democracy@localhost:5434/democracy_os
DATABASE_HOST=localhost
DATABASE_PORT=5434
DATABASE_NAME=democracy_os
DATABASE_USER=democracy
DATABASE_PASSWORD=democracy

# Redis
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379

# MinIO / S3
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=democracy-os
MINIO_USE_SSL=false

# API
API_PORT=3001
API_HOST=localhost
API_BASE_URL=http://localhost:3001

# JWT
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Encryption
ENCRYPTION_KEY=change-this-to-a-secure-32-byte-key-in-production

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_NAME=Democracy OS

# Node environment
NODE_ENV=development

# Email (for future implementation)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@democracy-os.local

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
