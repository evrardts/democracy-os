{
  "buildCommand": "pnpm install --frozen-lockfile --prod=false && pnpm -w run build:vercel && mkdir -p public",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "includeFiles": "apps/api/dist/**,packages/*/dist/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
